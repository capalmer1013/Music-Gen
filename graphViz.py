import matplotlib.pyplot as plt
import networkx as nx
import matplotlib


# G.add_edge(2,3,weight=0.9) # specify edge data

# for i in range(len(rhythmMatrix)):
#     for j in range(len(rhythmMatrix)):
#         if float(rhythmMatrix[i][j].numerator)/float(rhythmMatrix[i][j].denominator) != 0:
#             G.add_edge(i, j, weight=float(rhythmMatrix[i][j].numerator)/float(rhythmMatrix[i][j].denominator))
# nx.draw_circular(G)
#
# plt.show()